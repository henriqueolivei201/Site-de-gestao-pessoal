# TODO (Projeto 2 - Site de Gestão Pessoal)

- [ ] Implementar `renderPontuacao()` em `script.js` (usar `gerarHistoricoPontuacao()`, `getRankAtual()` e Canvas `pontuacao-chart`).
- [ ] Dentro do `RANKS.forEach`, adicionar a lógica do Hall da Fama exatamente como solicitado (sem alterar `carregarHallFama`).
- [ ] Garantir que ao trocar para a aba `pontuacao`, o código não lance erro (confirmar que `renderPontuacao()` existe).
- [ ] Rodar/validar (abrir `index.html` e testar navegação para `Pontuação`).

