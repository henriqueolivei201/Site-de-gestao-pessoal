# TODO - Estatísticas/Auto-expiração

- [x] Ajustar `setupMidnightCheck()` para não sobrescrever metas válidas com `[]` (guarda se localStorage não é array válido e mantém quando transformação não faz sentido).
- [x] Adicionar debug em `renderEstatisticas()` para logar os valores/formatos vindos do `localStorage` para `metas_diario`, `metas_semanal`, `metas_mensal`, `metas_anual` e `calendario_dias`.

