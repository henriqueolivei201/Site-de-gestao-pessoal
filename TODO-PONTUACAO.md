# TODO - Pontuação

- [ ] Implementar `renderPontuacao()` em `script.js`.
- [ ] Exibir: rank atual, total de pontos, e gráfico no canvas `pontuacao-chart`.
- [ ] Inserir lógica de Hall da Fama desbloqueado dentro do loop `RANKS.forEach`, com `id: rank_${r.nome}`.
- [ ] Não alterar `carregarHallFama()` existente.

